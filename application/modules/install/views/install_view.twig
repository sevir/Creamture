{% extends "layout/install_layout.twig" %}

{% block content %}
<div class="span12 center">
	<img src="{{img_path}}creamture_logo.png" width="400" />
</div>
<div class="row">
	<div class="offset2 span8">
		<p class="lead resume center">{% trans "The modern framework for building big web applications, built-in over the latest practices and standards HTML5, CSS3 and JavaScript patterns" %}</p>
		<div class="offset1 span9 features">
			<h5>{% trans "It includes all this features:" %}</h5>
			<ul class="unstyled">
				<li>
					<i class="icon-ok"></i> {% trans "CodeIgniter Reactor" %}
					<p>{% trans "Based in the latest community CodeIgniter version." %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "REST Server" %}
					<p>{% trans "Build the better REST API for your application, used with JS Backbone applications." %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "Twig template library" %}
					<p>{% trans "Flexible, fast and secure template engine for PHP. One of the more advanced template engine of the hand of Symfony creators." %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "Sparks package management system" %}
					<p>{% trans "Install easily CI libraries and extensions, extend your framework rapidly with thousand of features." %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "Command Line CRUD &amp; functions" %}
					<p>{% trans "Creamator is the CLI manager that you can extend easily with functions. Try now writing: <pre>./creamture creamator help.</pre>" %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "HMVC Modules" %}
					<p>{% trans "Write your application with modules, refactorize your app!!" %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "Bitauth light user management" %}
					<p>{% trans "Construct your application with support of users, roles, groups and permissions." %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "AJAX library" %}
					<p>{% trans "Deploy fast AJAX responses with the AJAX sparks in JSON or XML format with Cross-Domain AJAX support." %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "PHP Error improvement visualization" %}
					<p>{% trans "PHP Error Library improves the error visualization also in the AJAX calls." %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "SimpleTester Unit Testing" %}
					<p>{% trans "Write your unit testing for your code." %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "CI PHP Language Editor" %}
					<p>{% trans "An online Language Editor for CI, edit easily your language files with automatic Google translation for help us." %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "Automatic environment configuration" %}
					<p>{% trans "Set the environment based in the operating system or the computer name. No more changes by hand, prepare your code for all the environments and run automatically with the correct routine." %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "GetText i18n Support with Command Line generation" %}
					<p>{% trans "Generation of PO files and support within Twig templates. Command line creamator commands for PO generation." %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "Assets management with LESS, CoffeeScript, CSS and JS Minify" %}
					<p>{% trans "Supported in Twig and CI standard views. You can write your CSS in LESS syntax, use variables, and unify files. Write in CoffeScript and generate new compilation cache when your source files has been changed." %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "HTML5 Boilerplate" %}
					<p>{% trans "Prepared for HTML5, with robots.txt, Flash crossdomain file, humans.txt,..." %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "Twitter Boostrap CSS Framework" %}
					<p>{% trans "Frontend framework for easy web development." %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "jQuery" %}
					<p>{% trans "The JS DOM manipulation library by default." %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "PlUpload" %}
					<p>{% trans "Upload files with size limit, automatically trip the file using Flash, HTML5, and compatible with old browsers." %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "Channels.js" %}
					<p>{% trans "Write JavaScript modules communicating all the components within iframes, windows or in the same page. Go multi-desktop web apps!!" %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "Modernizr.js" %}
					<p>{% trans "HTML5 compatibility for old browsers." %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "Require.js" %}
					<p>{% trans "AMD Support for your JavaScript Applications." %}</p>
				</li>
				<li>
					<i class="icon-ok"></i> {% trans "Backbone &amp; Underscore js library" %}
					<p>{% trans "Build true JavaScript Web Applications with REST backend." %}</p>
				</li>
			</ul>
		</div>
	</div>
</div>
{% endblock %}